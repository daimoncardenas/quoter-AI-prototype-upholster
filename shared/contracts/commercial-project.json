{
  "_nota": "La general (docs/quote-request.interface.ts, QuoteRequest) peluqueada para ESTA rama: los campos que su caso produce, con su tipo, sin valores de nadie. Los del sistema (id, fecha, vendedor, estado) no viajan en la petición: los pone el cotizador al crear la cotización.",
  "schemaVersion": 1,
  "branch": "commercial-project",
  "from": "QuoteRequest",
  "serviceLine": "proyecto-comercial",
  "fields": {
    "furniture": {
      "type": "string",
      "note": "string?"
    },
    "photoIds": "string[]",
    "budget": "number?",
    "customerCity": "string",
    "serviceCity": "string",
    "delivery": {
      "method": "\"sede\" | \"envio\" | \"asesor\"",
      "office": "string?"
    },
    "contact": {
      "name": "string",
      "email": "string",
      "phone": "string",
      "authorized": "boolean"
    },
    "clientEstimate": {
      "consumption": {
        "min": "number",
        "max": "number",
        "unit": "string"
      },
      "price": {
        "min": "number",
        "max": "number",
        "currency": "\"COP\""
      }
    }
  },
  "required": [
    "furniture",
    "photoIds",
    "delivery",
    "customerCity",
    "serviceCity",
    "contact"
  ],
  "system": [
    "id",
    "date",
    "seller",
    "sellerId",
    "status",
    "submittedAt"
  ]
}
