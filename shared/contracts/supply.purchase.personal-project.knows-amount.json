{
  "_nota": "La general (docs/quote-request.interface.ts, QuoteRequest) peluqueada para ESTA rama: los campos que su caso produce, con su tipo, sin valores de nadie. Los del sistema (id, fecha, vendedor, estado) no viajan en la petición: los pone el cotizador al crear la cotización.",
  "schemaVersion": 1,
  "branch": "supply.purchase.personal-project.knows-amount",
  "from": "QuoteRequest",
  "serviceLine": "suministro-tela",
  "route": "compra",
  "purpose": "mueble",
  "knowledge": "metros",
  "fields": {
    "preferences": {
      "needs": "string[]",
      "style": "string",
      "colorRange": "string"
    },
    "fabric": {
      "fabricId": "number",
      "reference": "string"
    },
    "purchase": [
      {
        "fabricId": "number",
        "reference": "string",
        "quantity": "number",
        "unit": "\"m\" | \"roll\""
      }
    ],
    "customerCity": "string",
    "serviceCity": "string",
    "delivery": {
      "method": "\"sede\" | \"envio\" | \"asesor\"",
      "office": "string?"
    },
    "contact": {
      "name": "string",
      "email": "string",
      "phone": "string",
      "authorized": "boolean"
    },
    "billing": {
      "modelo": "string",
      "tecnico": "string",
      "consumo": "[number, number]",
      "facturable": "[number, number]",
      "compra": "[number, number]",
      "sobrante": "[number, number]"
    },
    "price": "[number, number]",
    "clientEstimate": {
      "consumption": {
        "min": "number",
        "max": "number",
        "unit": "string"
      },
      "price": {
        "min": "number",
        "max": "number",
        "currency": "\"COP\""
      }
    }
  },
  "required": [
    "fabric",
    "purchase",
    "delivery",
    "customerCity",
    "serviceCity",
    "contact"
  ],
  "system": [
    "id",
    "date",
    "seller",
    "sellerId",
    "status",
    "submittedAt"
  ]
}
