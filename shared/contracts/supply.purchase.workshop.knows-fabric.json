{
  "_nota": "La general (docs/quote-request.interface.ts, QuoteRequest) peluqueada para ESTA rama: los campos que su caso produce, con su tipo, sin valores de nadie. Los del sistema (id, fecha, vendedor, estado) no viajan en la petición: los pone el cotizador al crear la cotización.",
  "schemaVersion": 1,
  "branch": "supply.purchase.workshop.knows-fabric",
  "from": "QuoteRequest",
  "serviceLine": "suministro-tela",
  "route": "compra",
  "purpose": "taller",
  "knowledge": "tela",
  "fields": {
    "furniture": {
      "type": "string",
      "note": "string?"
    },
    "photoIds": "string[]",
    "measurements": {
      "width": "number",
      "height": "number",
      "depth": "number",
      "seats": "string?",
      "cushions": "number | string | null",
      "coverage": "string"
    },
    "purchase": [
      {
        "fabricId": "number",
        "reference": "string",
        "quantity": "number",
        "unit": "\"m\" | \"roll\""
      }
    ],
    "customerCity": "string",
    "serviceCity": "string",
    "delivery": {
      "method": "\"sede\" | \"envio\" | \"asesor\"",
      "office": "string?"
    },
    "contact": {
      "name": "string",
      "email": "string",
      "phone": "string",
      "authorized": "boolean"
    },
    "price": "[number, number]",
    "clientEstimate": {
      "consumption": {
        "min": "number",
        "max": "number",
        "unit": "string"
      },
      "price": {
        "min": "number",
        "max": "number",
        "currency": "\"COP\""
      }
    }
  },
  "required": [
    "furniture",
    "photoIds",
    "measurements",
    "purchase",
    "delivery",
    "customerCity",
    "serviceCity",
    "contact"
  ],
  "system": [
    "id",
    "date",
    "seller",
    "sellerId",
    "status",
    "submittedAt"
  ]
}
